install riêng torch cho gpu
pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121

tải huggingface_hub cho download_model.py
pip install huggingface_hub

Chuẩn bị trước khi chạy sv
	- cài venv và activate sts (py version có thể lấy phiên bản > 3.10) riêng do incompitable về một số lib với venv của rasa nếu gộp chung
	
	- sts
		- pip install requirements.txt

		- model tts:
            if DEVICE == "cpu":
                tts = Vieneu(
                    mode="v3turbo"
                )
            else:
                tts = Vieneu(
                    mode="turbo_gpu"
                )

            # v3turbo (Mặc định): Sử dụng V3TurboVieNeuTTS (48 kHz, chạy CPU qua ONNX Runtime không cần torch, GPU dùng PyTorch).
            # remote hoặc api: Sử dụng RemoteVieNeuTTS (chạy qua API).
            # fast hoặc gpu: Sử dụng FastVieNeuTTS (dùng GPU-LMDeploy).
            # turbo: Sử dụng TurboVieNeuTTS.
            # turbo_gpu: Sử dụng TurboGPUVieNeuTTS.
            # xpu: Sử dụng XPUVieNeuTTS (dành cho GPU Intel, yêu cầu cài đặt driver và torch.xpu).
            # standard: Sử dụng VieNeuTTS (CPU/GPU-GGUF).

		- các đối số của Vieneu: 
			- voice:
				[
                    "Minh Đức"  
                    "Ánh Hà"    
                    "Quốc Mạnh" 
                    "Anh Nhi"   
                    "Ánh Hương" 
                    "Phạm Tuyên"    
                    "Vân Uyên"  
                    "Anh Khang" 
                    "Trúc Ly"   
                    "Tố Linh"   
				]
			- style:
				"tu nhien"
				"tin tuc"
				"doc_truyen"

chạy file main.py trong sts ( có thể tắt scripts rasa trong __main__)